pub fn held(bag) {
    info(format!("held {:?}", bag));
}

pub fn dropped(bag) {
    info(format!("dropped {:?}", bag));
}

pub fn left(bag) {
    info(format!("left"));
}

pub fn arrived(bag) {
    info(format!("arrived"));
}

pub fn handlers() {
    #{
        "carrying": #{
            "held": held,
            "dropped": dropped
        },
        "moving": #{
            "left": left,
            "arrived": arrived
        }
    }
}

pub fn before(perform) {
    // info(format!("before! {:?}", perform));
    return Some(perform)
}

pub fn after(effect) {
    // info("after!");
    return effect;
}