pub fn held(bag) {
    info(format!("held {:?}", bag));
}

pub fn dropped(bag) {
    info(format!("dropped {:?}", bag));
    info(format!("dropped {:?}", bag.item()));
    info(format!("dropped {:?}", bag.area()));
    info(format!("dropped {:?}", bag.living()));
}

pub fn left(bag) {
    info(format!("left"));
}

pub fn arrived(bag) {
    info(format!("arrived"));
}

pub fn handlers() {
    #{
        "carrying": #{
            "held": held,
            "dropped": dropped
        },
        "moving": #{
            "left": left,
            "arrived": arrived
        }
    }
}

pub fn before(perform) {
    return Some(perform)
}

pub fn after(effect) {
    return effect;
}