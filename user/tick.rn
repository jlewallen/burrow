pub fn user() {
    let thing = Thing::new();
    info(format!("thing={:?}", thing));
}

pub fn have_surroundings() {
    info(format!("surroundings"));
}

// These should fail eventually unless there's an Entity context.

// #[dropping]
pub fn dropping() {
    true
}

// #[dropped]
pub fn dropped() {
}

// #[holding]
pub fn holding() {
    true
}

// #[held]
pub fn held() {
}

// #[observed]
pub fn observed() {
}