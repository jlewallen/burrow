[package]
name = "plugin-example-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
bincode = { version = "2.0.0-rc", features = ["serde"] }

[dependencies.wasm-sys]
path = "../../plugins/wasm-sys"

# [profile.release]
# lto = true
